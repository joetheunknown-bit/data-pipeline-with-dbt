version: 2

sources:
  - name: aiven_mongo
    description: "Raw data imported from MongoDB via Aiven"
    database: defaultdb
    schema: ecommercc

    tables:
      - name: orders
        description: "Raw landing table for ecommerce order transactions"
        columns:
          - name: order_id
          - name: customer_id
          - name: order_date
          - name: total_amount
          # The rest of your columns will follow here